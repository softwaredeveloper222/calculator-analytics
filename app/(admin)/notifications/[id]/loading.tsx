import { PageLoadingSpinner } from "@/components/PageLoadingSpinner";

export default function NotificationEditLoading() {
  return <PageLoadingSpinner label="Loading editor…" fullScreen={false} />;
}
